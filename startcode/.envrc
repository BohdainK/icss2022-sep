#!/usr/bin/env bash
# .envrc - set project-local JAVA_HOME to Java 13 (requires direnv)
export JAVA_HOME=$(/usr/libexec/java_home -v 13 2>/dev/null || true)
if [ -n "$JAVA_HOME" ]; then
  export PATH="$JAVA_HOME/bin:$PATH"
fi
